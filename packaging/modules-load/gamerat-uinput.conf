# Ensure the uinput module is loaded at boot.
#
# Most modern distros load it automatically when a process opens
# /dev/uinput, but some minimal installs ship the kernel module
# without the udev rule that triggers autoload. This file makes the
# load explicit so gamerat-daemon's first attempt to open
# /dev/uinput doesn't race the on-demand loader.
uinput
