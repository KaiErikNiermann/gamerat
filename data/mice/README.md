# Mouse SVG diagrams

Per-device SVG illustrations used by the GUI to render labeled
button-mapping overlays. Sourced from the `libratbag/piper` project's
`data/svgs/` directory.

See [`ATTRIBUTION.md`](ATTRIBUTION.md) for provenance and licensing.
